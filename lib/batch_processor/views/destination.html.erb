<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <title>Lonely Planet | <%= destination.name %></title>
    <link href="static/all.css" media="screen" rel="stylesheet" type="text/css">
  </head>

  <body>
    <div id="container">
      <div id="header">
        <div id="logo"></div>
        <h1>Lonely Planet: <%= destination.name %></h1>
      </div>

      <div id="wrapper">
        <div id="sidebar">
          <div class="block">
            <h3>Navigation</h3>
            <div class="content">
              <div class="inner">
                <ul class="navigation">
                <% taxonomy.ancestors.each do |node| %>
                  <li>
                    <a href="<%= node.file_name %>"><%= node.name %></a>
                  </li>
                <% end %>

                <li>
                  <a class="active" href="<%= destination.file_name %>"><%= destination.name %></a>
                </li>

                <% taxonomy.children.each do |node| %>
                  <li>
                    <a href="<%= node.file_name %>"><%= node.name %></a>
                  </li>
                <% end %>
                </ul>
              </div>
            </div>
          </div>
        </div>

        <div id="main">
          <div class="block">

            <div class="secondary-navigation">
              <ul>
                <li class="first">
                  <a href="<%= destination.file_name %>"><%= destination.name %></a>
                </li>
              </ul>
              <div class="clear"></div>
            </div>

            <div class="content">
              <div class="inner">

              <h1><%= destination.name %></h1>

                <% destination.sections.each do |section| %>
                  <%= render_section section %>
                  <hr />
                <% end  %>

              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </body>
</html>
